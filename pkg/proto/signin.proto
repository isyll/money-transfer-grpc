syntax = "proto3";

service Signin {
    rpc Translate (SigninRequest) returns (SigninResponse);
}

message SigninRequest {
    string username = 1;
    string password = 2;
}

message SigninResponse {
    string accessToken = 1;
    string refreshToken = 2;
}
